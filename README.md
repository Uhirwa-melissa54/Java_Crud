# Java CRUD Application with PostgreSQL and Swing
## Overview
This is a **Java Swing GUI application** that demonstrates **CRUD operations** (Create, Read, Update, Delete) using **PostgreSQL** as the database. The application manages **student records**, including:

- Full name  
- Location  
- School  

Users can add new students, view all students in a table, update existing records, and delete students directly from the GUI
